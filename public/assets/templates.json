{
  "_readme": "One entry per template. 'fields' lists every id the generation engine should treat as a data-binding target — i.e. text to replace or an image to inject. Purely decorative/structural ids (gradients, clip-paths, corner ornaments, wrapper groups, static design labels) are intentionally excluded; the SVGs contain many more ids than are listed here, and that's expected. type 'text' = replace element's text content. type 'image' = set the element's image href to the uploaded file, scaled to fit its bounding box. 'image_target_id' (new): when an image field's id sits on a wrapping <g> rather than directly on the <image> element that carries the href, this names the actual <image> id to write to — render.js falls back to field.id when this is absent. required=false fields may be left blank/skipped per FR-2.7 (optional fields render blank, template layout never shifts).",
  "templates": [
    {
      "id": "exclusive-completion",
      "name": "Exclusive Completion",
      "file": "Exclusive_Completion_certificate.svg",
      "canvas": {
        "width": 3508,
        "height": 2481
      },
      "notes": "FIX (Aug 2026): opt-seal-slot now declares hide_container: 'seal-block' — the seal artwork (medallion, arc text, shield emblem) was previously a set of bare sibling elements with no wrapping group, so it rendered on every certificate even when no seal image was uploaded. The SVG was updated to wrap that artwork in <g id=\"seal-block\">.",
      "fields": [
        { "id": "institution-name", "type": "text", "required": true },
        { "id": "opt-subtitle", "type": "text", "required": false },
        { "id": "recipient-name", "type": "text", "required": true },
        { "id": "course-title", "type": "text", "required": true },
        { "id": "start-date", "type": "text", "required": true },
        { "id": "end-date", "type": "text", "required": true },
        { "id": "award-day", "type": "text", "required": true },
        { "id": "award-month", "type": "text", "required": true },
        { "id": "award-year", "type": "text", "required": true },
        { "id": "certificate-number", "type": "text", "required": true, "auto": true },
        { "id": "logo-slot", "type": "image", "required": true },
        { "id": "signature-1", "type": "image", "required": true },
        { "id": "signature-1-caption", "type": "text", "required": true },
        { "id": "signature-1-title", "type": "text", "required": true },
        { "id": "signature-2", "type": "image", "required": false, "hide_container": "signature-2-block" },
        { "id": "opt-signature-2-caption", "type": "text", "required": false },
        { "id": "opt-signature-2-title", "type": "text", "required": false },
        { "id": "opt-seal-slot", "type": "image", "required": false, "hide_container": "seal-block" }
      ]
    },
    {
      "id": "modern-excellence",
      "name": "Modern Excellence",
      "file": "Modern_Excellence_certificate.svg",
      "canvas": {
        "width": 3508,
        "height": 2481
      },
      "notes": "No course-title or start-date/end-date fields by design — treated as a general achievement award, not tied to a specific course or date range.",
      "fields": [
        { "id": "institution-name", "type": "text", "required": true },
        { "id": "opt-subtitle", "type": "text", "required": false },
        { "id": "recipient-name", "type": "text", "required": true },
        { "id": "award-day", "type": "text", "required": true },
        { "id": "award-month", "type": "text", "required": true },
        { "id": "award-year", "type": "text", "required": true },
        { "id": "certificate-number", "type": "text", "required": true, "auto": true },
        { "id": "logo-slot", "type": "image", "required": true },
        { "id": "signature-1", "type": "image", "required": true },
        { "id": "signature-1-caption", "type": "text", "required": true },
        { "id": "signature-1-title", "type": "text", "required": true },
        { "id": "signature-2", "type": "image", "required": false, "hide_container": "signature-2-block" },
        { "id": "opt-signature-2-caption", "type": "text", "required": false },
        { "id": "opt-signature-2-title", "type": "text", "required": false },
        { "id": "opt-seal-slot", "type": "image", "required": false }
      ]
    },
    {
      "id": "premium-training",
      "name": "Premium Training",
      "file": "Premium_Traning_certificate.svg",
      "canvas": {
        "width": 3508,
        "height": 2481
      },
      "notes": "award-date-line is a single text field (e.g. 'Granted this 14th day of June, 2026') rather than separate day/month/year fields — different design choice from the other templates, intentionally left as-is.",
      "fields": [
        { "id": "institution-name", "type": "text", "required": true },
        { "id": "opt-subtitle", "type": "text", "required": false },
        { "id":
